One-line: The brand's radiant gradient surface — sky-blue easing into gold/orange; use `panel` for the rounded hero card.

```jsx
<Gradient panel style={{ padding: '4rem' }}>
  <Heading as="h1" size="xl">More than just glass.</Heading>
</Gradient>
```
`soft` swaps to the pale wash used behind full page sections / the CTA band.
