One-line: Feature tile for bento-style grids — optional media at top, then eyebrow, heading and body copy.

```jsx
<BentoCard eyebrow="Showers" title="Frameless enclosures" media="/shower.jpg">
  Made to suit every unique bathroom — stylish, easy-clean, elegant lines.
</BentoCard>
```
Pass `dark` to use inside the navy section. Grid these with CSS grid + `gap`.
