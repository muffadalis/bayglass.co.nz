One-line: Space Grotesk display heading; `xl` for hero, `lg` for section titles, down to `sm` for cards.

```jsx
<Heading as="h1" size="xl">More than just glass.</Heading>
<Heading size="lg" dark>Meet leads where they are</Heading>
```
Pass `dark` on navy sections. Uses tight tracking + balanced wrapping automatically.
