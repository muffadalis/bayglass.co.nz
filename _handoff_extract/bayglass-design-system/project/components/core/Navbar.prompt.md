One-line: The Bayglass marketing navbar — wordmark, optional announcement pill, nav links and a CTA button. Place it inside the hero gradient panel.

```jsx
<Container>
  <Navbar cta={{ label: 'Get a quote', href: '/contact' }} />
</Container>
```
Pass `links`, `banner` (or `null` to hide), and `cta` to configure.
